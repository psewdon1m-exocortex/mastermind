* text=auto eol=lf
docs/policy/** -text
# Unified diff context deliberately has a space before Go's tab indentation.
# Preserve bytes; validate these files by applying them to their locked bases.
integrations/patches/** -text -whitespace
*.woff2 binary
*.doc binary
